# DataWhisper API - Environment Variables Template
# Bu dosyayı .env olarak kopyalayın ve değerlerinizi girin
# .env dosyası asla git'e commit edilmemelidir

# ============================================
# DATABASE CREDENTIALS
# ============================================
# PostgreSQL veritabanı şifresi (GÜÇLÜ ŞİFRE kullanın)
DB_PASSWORD=your_secure_postgres_password_here

# MongoDB root şifresi (GÜÇLÜ ŞİFRE kullanın)
MONGO_PASSWORD=your_secure_mongodb_password_here

# ============================================
# API CONNECTION STRINGS
# ============================================
# PostgreSQL connection string
# Format: Host=postgres;Database=datawhisper;Username=datawhisper_user;Password=YOUR_PASSWORD
CONNECTION_STRING_DEFAULT=Host=postgres;Database=datawhisper;Username=datawhisper_user;Password=${DB_PASSWORD}

# MongoDB connection string
# Format: mongodb://username:password@host:port/database?authSource=admin
CONNECTION_STRING_MONGODB=mongodb://datawhisper_user:${MONGO_PASSWORD}@mongodb:27017/datawhisper_analytics?authSource=admin

# ============================================
# AI SERVICE CONFIGURATION
# ============================================
# AI Service URL (External, bağımsız servis)
# Local development için:
AI_SERVICE_URL=http://localhost:5003
# Production için (GitHub secret: PROD_AI_SERVICE_URL):
# AI_SERVICE_URL=http://160.20.111.45:5003

# Not: OpenAI API Key artık AI service'in kendi repo'sunda yönetiliyor
# Bu API projesi sadece AI service'e HTTP istek atar

# ============================================
# APPLICATION SETTINGS
# ============================================
# ASP.NET Core Environment
ASPNETCORE_ENVIRONMENT=Development

# Large dataset threshold (row count)
LARGE_DATASET_THRESHOLD=10

# Cache TTL in minutes
CACHE_TTL_MINUTES=5

# ============================================
# GÜVENLİK NOTLARI
# ============================================

# ============================================
# BUILD & DEPLOYMENT
# ============================================
# Build version tag
VERSION=latest

# Build date (otomatik doldurulur)
BUILD_DATE=

# Version control reference (otomatik doldurulur)
VCS_REF=
# 1. Tüm şifreler en az 16 karakter, büyük/küçük harf, sayı ve özel karakter içermeli
# 2. Production'da farklı şifreler kullanın
# 3. Şifreleri düzenli olarak rotation yapın
# 4. .env dosyasını asla commit etmeyin (.gitignore'da var)
# 5. Production için GitHub Secrets veya Docker Secrets kullanın
# 6. OpenAI API Key'inizi paylaşmayın

# ============================================
# ÖRNEK GÜÇLÜ ŞİFRE OLUŞTURMA
# ============================================
# OpenSSL ile: openssl rand -base64 32
# Python ile: python -c "import secrets; print(secrets.token_urlsafe(32))"
# PowerShell ile: -join ((48..57) + (65..90) + (97..122) | Get-Random -Count 32 | % {[char]$_})
